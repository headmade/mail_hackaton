.row
  .small-12.columns
    %h1 New collage
    %ul.small-block-grid-4.collage-js
      - @images.each do |image|
        %li
          = image_tag image.image, class: 'th', data: {id:image.id}

    = render 'form'

    = link_to 'Back', collages_path
